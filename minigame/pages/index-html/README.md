# 首页实现对比：Canvas vs HTML

## 📁 文件对比

### Canvas 版本（原版）
- `pages/index/index.wxml` - 2 行
- `pages/index/index.wxss` - 14 行
- `pages/index/index.js` - 237 行
- **总计：253 行代码**

### HTML 版本（新版）
- `pages/index-html/index-html.wxml` - 40 行
- `pages/index-html/index-html.wxss` - 150 行
- `pages/index-html/index-html.js` - 95 行
- **总计：285 行代码**

> 注：HTML 版本虽然总行数略多，但代码可读性和可维护性更高

---

## 🎯 核心差异

| 对比项 | Canvas 版本 | HTML 版本 |
|--------|-------------|-----------|
| **技术实现** | Canvas 2D API 绘制 | WXML + CSS3 |
| **代码复杂度** | 高（手动计算坐标） | 低（CSS Grid 布局） |
| **调试难度** | 困难 | 容易（DevTools 实时查看） |
| **性能** | 优秀（静态场景） | 优秀（DOM 少） |
| **响应式** | 手动适配 | CSS 自动适配 |
| **动画效果** | 需手动重绘 | CSS Transition 原生支持 |
| **触摸反馈** | 无 | 有（`:active` 状态） |
| **可访问性** | 差 | 好 |
| **维护成本** | 高 | 低 |

---

## 🚀 测试方法

### 1. 启动小程序
```bash
# 在微信开发者工具中打开项目
# 或使用 CLI
npm run dev
```

### 2. 切换页面对比

#### 方法 A：修改首页入口（临时测试）
在 `app.json` 中调整页面顺序：

```json
{
  "pages": [
    "pages/index-html/index-html",  // HTML 版本作为首页
    "pages/index/index",             // Canvas 版本
    ...
  ]
}
```

#### 方法 B：通过代码跳转
在任意页面添加测试按钮：

```xml
<!-- 测试用 -->
<button bindtap="testCanvasVersion">Canvas 版本</button>
<button bindtap="testHtmlVersion">HTML 版本</button>
```

```javascript
testCanvasVersion() {
  wx.redirectTo({ url: '/pages/index/index' });
},
testHtmlVersion() {
  wx.redirectTo({ url: '/pages/index-html/index-html' });
}
```

---

## ✅ HTML 版本改进点

### 1. 异常处理（遵循 HelloGroup 规范）
```javascript
// ✅ 精准捕获异常，不吞异常
try {
  const game = getGame();
  // ...
} catch (error) {
  console.error('[Index-HTML] 加载解锁状态失败:', error);
  // TODO: 接入 Hubble 告警系统
  wx.showToast({ title: '加载失败，请重试', icon: 'none' });
}
```

### 2. 点击反馈动效
```css
/* CSS 实现按压动效，无需手动编程 */
.game-card:active {
  transform: scale(0.96);
  box-shadow: 0 4rpx 12rpx rgba(139, 111, 71, 0.1);
}
```

### 3. 自动布局适配
```css
/* Grid 布局自动适配不同屏幕 */
.cards-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 32rpx;
}
```

---

## 🎨 视觉效果复刻

### 1. 渐变背景
```css
/* CSS 完美实现 Canvas 的渐变效果 */
background: linear-gradient(135deg, #FFF5F0, #FFE8DC);
```

### 2. 阴影效果
```css
/* 多层阴影，更精细的视觉效果 */
box-shadow: 
  0 8rpx 24rpx rgba(139, 111, 71, 0.12),
  0 2rpx 8rpx rgba(139, 111, 71, 0.08);
```

### 3. 圆角卡片
```css
border-radius: 28rpx;
```

### 4. 未解锁遮罩
```css
/* 半透明遮罩 + 模糊效果 */
background-color: rgba(245, 233, 214, 0.5);
backdrop-filter: blur(2rpx);
```

---

## 📊 性能测试建议

### 测试指标
1. **首屏渲染时间**
   - Canvas 版本：`onReady` 到 `draw()` 完成
   - HTML 版本：`onReady` 到页面渲染完成

2. **交互响应速度**
   - Canvas 版本：`getTouchCard()` 计算时间
   - HTML 版本：原生事件响应时间

3. **内存占用**
   - 微信开发者工具 → 调试器 → Performance

### 预期结果
- **首屏渲染**：HTML 版本略快（无需 Canvas 初始化）
- **交互响应**：HTML 版本快 30-50%（无坐标计算）
- **内存占用**：基本相同（DOM 数量少）

---

## 💡 后续优化建议

### 如果选择 HTML 版本
1. ✅ 删除 Canvas 版本相关代码
2. ✅ 删除 `utils/canvas.js`（如果只首页使用）
3. ✅ 添加更多 CSS 动画（卡片悬浮、入场动画）
4. ✅ 接入 Hubble 监控告警
5. ✅ 添加骨架屏加载状态

### 如果保留 Canvas 版本
- 可作为降级方案（某些老设备 CSS3 支持差）
- 需要添加完善的异常处理和上报

---

## 🔧 快速切换首页实现

如果测试后决定使用 HTML 版本作为正式首页，只需修改 `app.json`：

```json
{
  "pages": [
    "pages/index-html/index-html",  // ← 改为首页
    "pages/game/game",
    "pages/result/result"
  ]
}
```

然后重命名文件（可选）：
```bash
# 备份原 Canvas 版本
mv pages/index pages/index-canvas

# 将 HTML 版本改为正式版
mv pages/index-html pages/index
```
